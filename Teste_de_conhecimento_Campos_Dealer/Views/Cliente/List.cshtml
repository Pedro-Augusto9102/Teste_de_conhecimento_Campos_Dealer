@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<Teste_de_conhecimento_Campos_Dealer.Models.Entities.Cliente>
<h1>Students</h1>
@if(TempData["AlertMessage"] is not null)
{
    <div class="alert alert-success">
        <strong>Sucesso!</strong>@TempData["AlertMessage"]
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var cliente in Model)
        {
            <tr>
                <td>@cliente.Id</td>
                <td>@cliente.Name</td>
                <td>@cliente.City</td>
                <td>
                    <a class="btn btn-primary" asp-controller="Cliente" asp-action="Edit" asp-route-id="@cliente.Id">Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>